spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:dpg-da6ste90er6s73dbv0qg-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:spotitdatabse}?currentSchema=public&sslmode=${DB_SSLMODE:require}
    username: ${DB_USER:spotituser}
    password: ${DB_PASSWORD:7ApipEekTQZMtWR4S6UwiMeI5tOcUpyX}
    type: com.zaxxer.hikari.HikariDataSource
  jpa:
    show-sql: true

logging:
  level:
    com.spotit.api: DEBUG
